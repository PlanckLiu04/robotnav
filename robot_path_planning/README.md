# RobotNav：可交互的 2D 机器人路径规划与仿真控制平台

## 项目简介

RobotNav 是一个使用 Python + Pygame 开发的二维机器人路径规划与仿真控制项目。

当前第一阶段已经完成了一个可交互的 2D 网格地图原型：用户可以在窗口中设置障碍物、起点和终点，并通过 BFS 广度优先搜索算法规划从起点到终点的路径。

项目最终目标是做成一个适合初学者学习和展示的机器人导航仿真平台，逐步包含地图编辑、路径规划、搜索过程可视化、机器人沿路径运动、PID 控制以及算法效果对比等功能。

## 当前已实现功能

- 使用 Pygame 创建可运行的 2D 窗口。
- 在窗口中绘制规则网格地图。
- 支持鼠标点击交互。
- 支持障碍物编辑：
  - 按 `1` 切换到障碍物模式。
  - 点击空白格子会变成黑色障碍物。
  - 再次点击黑色障碍物会取消障碍。
- 支持设置起点：
  - 按 `2` 切换到起点模式。
  - 点击格子设置绿色起点。
  - 起点只有一个，位置为最后一次点击的位置。
- 支持设置终点：
  - 按 `3` 切换到终点模式。
  - 点击格子设置红色终点。
  - 终点只有一个，位置为最后一次点击的位置。
- 支持 BFS 路径规划：
  - 按 `Space` 空格键开始搜索路径。
  - 如果起点和终点之间存在可通行路线，会显示蓝色路径。

## 操作说明

运行项目：

```bash
python3 main.py
```

如果你的系统中 `python3` 命令不可用，可以尝试：

```bash
python main.py
```

窗口打开后：

```text
按 1：障碍物编辑模式
按 2：设置起点模式
按 3：设置终点模式
按 Space：执行 BFS 路径规划
关闭窗口：退出程序
```

颜色含义：

```text
浅色格子：可通行区域
黑色格子：障碍物
绿色格子：起点
红色格子：终点
蓝色格子：规划得到的路径
```

## 当前代码结构

当前阶段代码通常集中在 `main.py` 中，便于初学者理解完整流程。

主要变量：

```text
WIDTH, HEIGHT：窗口宽度和高度
CELL_SIZE：每个网格单元的像素大小
ROWS, COLS：地图的行数和列数
screen：Pygame 绘图窗口
clock：控制刷新率的时钟对象
obstacles：保存障碍物格子的集合
start：保存起点格子
goal：保存终点格子
path：保存 BFS 找到的路径
mode：当前鼠标点击模式
```

主要函数：

```text
draw_grid()：绘制网格线
draw_cell(cell, color)：绘制某一个格子
draw_obstacles()：绘制所有障碍物
get_neighbors(cell)：获取当前格子上下左右的可通行邻居
bfs(start, goal)：使用广度优先搜索寻找路径
```

主循环负责：

```text
读取键盘事件
读取鼠标点击事件
更新当前模式
添加或删除障碍物
设置起点和终点
按空格时执行路径搜索
重新绘制整个窗口
```

## BFS 算法说明

BFS 是 Breadth-First Search，也就是广度优先搜索。

在本项目中，BFS 会从起点开始，一层一层向外扩散搜索：

```text
第 0 层：起点
第 1 层：起点上下左右相邻的格子
第 2 层：再往外一圈的格子
...
直到找到终点
```

因为当前网格中每移动一格的代价相同，所以 BFS 找到的路径是最短路径之一。

BFS 中用到的关键数据结构：

```text
queue：队列，保存接下来要访问的格子
visited：集合，记录已经访问过的格子
parent：字典，记录每个格子是从哪个上一个格子走来的，用于最后反向还原路径
```

## 第一阶段完成情况

第一阶段目标：

```text
搭建项目基础结构，创建 Pygame 窗口和 2D 网格画布，实现基础交互。
```

当前已经完成：

```text
Pygame 窗口
网格地图
障碍物添加和删除
起点设置
终点设置
BFS 路径搜索
路径显示
```

这意味着项目已经从一个空白窗口升级为一个简单可用的 2D 路径规划原型。

## 最终目标

项目最终希望逐步实现：

- 可交互的二维网格地图。
- 鼠标绘制和删除障碍物。
- 起点和终点设置。
- BFS 路径规划。
- A* 路径规划。
- 路径搜索过程可视化。
- 路径长度、访问节点数、运行时间统计。
- 可移动的机器人模型。
- 机器人沿路径点运动。
- PID heading controller 控制机器人朝向。
- 机器人轨迹显示。
- 不同路径规划算法对比。
- 更完整的界面控制区，例如开始、暂停、重置、清空地图等按钮。

## 后续改进方向

建议按下面顺序继续开发：

1. 增加重置功能

   例如按 `C` 清空障碍物、起点、终点和路径。

2. 增加找不到路径的提示

   当 BFS 返回空路径时，在窗口标题或控制台中提示 `No path found`。

3. 可视化 BFS 搜索过程

   不只显示最终路径，还可以显示 BFS 一圈一圈扩散访问过的格子。

4. 实现 A* 算法

   A* 比 BFS 更有方向感，更适合作为路径规划项目展示。

5. 拆分代码结构

   当前所有代码集中在 `main.py`，后续可以拆成：

   ```text
   main.py
   config.py
   grid_map.py
   planner.py
   renderer.py
   ```

6. 增加机器人运动仿真

   使用一个圆形或三角形表示机器人，让机器人沿蓝色路径逐格移动。

7. 增加 PID 控制

   在机器人不再逐格瞬移之后，加入位置、朝向、速度和转向控制。

8. 增加项目展示材料

   保存运行截图、录制演示视频，并补充项目开发日志。

## 推荐开发节奏

当前适合初学者的开发路线：

```text
第 1 阶段：窗口、网格、障碍物、起点、终点、BFS 路径
第 2 阶段：搜索过程可视化、重置功能、错误提示
第 3 阶段：A* 算法、算法对比
第 4 阶段：机器人模型和路径跟踪
第 5 阶段：PID 控制和运动仿真
第 6 阶段：界面优化、截图、视频、文档整理
```

## 给后续 AI 协作的说明

如果后续使用 AI 继续开发，请先阅读本文档，理解当前项目状态：

```text
技术路线：Python + Pygame
当前阶段：单文件 Pygame 原型
当前核心功能：网格编辑 + 起点终点设置 + BFS 路径规划
下一步优先级：重置功能、搜索过程可视化、A* 算法、代码模块化
项目最终方向：2D 机器人路径规划与 PID 导航控制仿真平台
```

开发时建议保持小步迭代：每次只新增一个清晰功能，运行成功后再继续下一步。
